// Deprecated by Sentry SDK 8 + Next.js 14. Init now lives in
// frontend/instrumentation.ts (the Next.js instrumentation hook).
// This file is intentionally empty — kept around only because Next's
// build still references the path during the Sentry plugin's
// auto-detection pass on some versions. Safe to delete once the
// plugin no longer mentions it.
export {};
