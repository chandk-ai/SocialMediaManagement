# Auto-generated for the Supabase project `SocialMediaManagement`.
# Rename this file to `.env.production.local` before deploying, OR pass these
# directly to `vercel env add` (see scripts/deploy_frontend.sh).

# ── Supabase (public — safe to expose in the browser) ────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://ukaulrxhegrqyfrirnkb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_FnQFGibT0kNMhZ5lFUA_7A_MUXpAD51

# ── Backend API (point at wherever you deploy FastAPI) ───────────────────
# For now, the frontend can run with Supabase Auth alone; you can fill this in
# once the backend is deployed on Render / Fly / Railway.
BACKEND_URL=
NEXT_PUBLIC_API_URL=

# ── NextAuth (server-side; never exposed to the browser) ─────────────────
# Generate with: openssl rand -hex 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://your-vercel-url.vercel.app

# ── Optional: Okta SSO (only if you prefer Okta over Supabase Auth) ──────
OKTA_ISSUER=
OKTA_CLIENT_ID=
OKTA_CLIENT_SECRET=
